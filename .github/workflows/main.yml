name: Trigger Jenkins

on:
  push:
    branches:
      - main

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Jenkins job
        run: |
          curl -X POST https://jenkins-geek.devops.de/job/deploy-blanc-site/build?token=mytoken
