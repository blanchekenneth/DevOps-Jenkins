name: Trigger Jenkins

on:
  push:
    branches:
      - main

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Jenkins job
        run: |
          curl -X POST http://blanc.geek-devops.de:8080/job/deploy-blanc-site/build?token=mytoken
